# City-mode crowd-carried scenario for GATE-A, using the real CADP cadp_0016.mp4
# clip (see junction_cam.yaml and PROGRESS.md for why cadp_0024's occluded
# rider-down clip was set aside in favor of this one).
scenario_id: scenario1
camera_config: configs/cameras/junction_cam.yaml
description: >
  City-mode crowd-formation scenario: a bystander crowd gathers and grows
  around a stopped bus over ~80s, with no clean two-vehicle collision ever
  visible to the camera. GATE-A requires crowd >= 0.5 while collision stays
  below its 0.9 override, fusion crossing the candidate threshold, and the
  incident reaching DISPATCHED with a single Confirm tap.
expected:
  type: city_crowd_only
  gt_event_time_s: 27.0
